import Agentic

// Legacy AgenticExecution authoring protocols were removed when Agentic.Tool
// became the canonical tool contract. Use Agentic.Source / Agentic.Result
// (or concrete Sendable + Codable + JSONSchemaProviding types) directly.
